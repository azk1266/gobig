{
  "devDependencies": {
    "@faker-js/faker": "^9.6.0",
    "chance": "^1.1.12",
    "cypress": "^14.2.0",
    "@shelex/cypress-allure-plugin": "^2.41.0",
    "cypress-allure-plugin": "^2.41.0"
  },
  "scripts": {
    "test": "cypress run",
    "allure:generate": "allure generate allure-results --clean -o allure-report",
    "allure:open": "allure open allure-report"
  }
}